import 'package:flutter/material.dart';

class AppColors {
  static const Color matteSlate = Color(0xFF1E1E1E);
  static const Color matteBlack = Color(0xFF121212);
  static const Color alabaster = Color(0xFFF5F5F5);
  static const Color alabasterCard = Color(0xFFFFFFFF);
  
  static const Color background = matteSlate;
  static const Color textPrimary = alabaster;
}
